# @schilderlabs/eslint-config

Internal ESLint configuration package for the Pi Town workspace.

In this monorepo the shared config is typically referenced via a relative import:

```js
import { config } from "../eslint-config/base.js"

export default [...config]
```
